% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/judge.R
\name{tw_judge_check}
\alias{tw_judge_check}
\title{A calibration-gated judge check}
\usage{
tw_judge_check(
  item,
  judge,
  calibration,
  dimension = "interpretation",
  label_map = c(C = "pass", P = "review", I = "problem")
)
}
\arguments{
\item{item}{A one-row data frame describing the analysis to judge (the columns
the judge reads, e.g. \code{input}, \code{target}).}

\item{judge}{A function \code{(data) -> character} returning one label per row --
the SAME judge calibrated with \code{yourhonor::calibrate_judge()}.}

\item{calibration}{That judge's \code{yourhonor_calibration}.}

\item{dimension}{Short name of what is being judged (e.g. \code{"interpretation"}).}

\item{label_map}{Named map from judge labels to tripwire statuses.}
}
\value{
A \code{tw_check} verdict.
}
\description{
Runs \code{judge} on a single analysis \code{item}, maps its label to a tripwire status,
and GATES that status by the judge's calibration: a \code{"caution"} judge cannot
return \code{problem} (it is capped to \code{review}); a \code{"not_calibrated"} judge is
advisory (never worse than \code{review}). Returns a \code{tw_check} that renders in a
health strip beside the deterministic checks.
}
\examples{
\dontshow{if (requireNamespace("yourhonor", quietly = TRUE)) withAutoprint(\{ # examplesIf}
gold <- data.frame(
  human_label = c("C", "C", "P", "I", "C", "P", "I", "C"),
  judge_label = c("C", "C", "C", "I", "C", "P", "P", "C")
)
cal <- yourhonor::calibrate_judge(gold, n_boot = 200, seed = 1)
judge <- function(data) rep("I", nrow(data)) # a judge that flags a problem
item <- data.frame(input = "claim: doubled; data: +12\%", target = "claim matches numbers?")
tw_judge_check(item, judge, cal, dimension = "interpretation")
\dontshow{\}) # examplesIf}
}
