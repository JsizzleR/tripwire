% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{tw_check_join}
\alias{tw_check_join}
\title{Check a join between two live data frames}
\usage{
tw_check_join(x, y, by, type = "inner")
}
\arguments{
\item{x, y}{Names of the left and right data frames in the R session.}

\item{by}{Key column name to join on (comma-separated for multiple columns).}

\item{type}{Join type: \code{"inner"}, \code{"left"}, \code{"right"}, or \code{"full"}.}
}
\value{
A list with \code{overall_status} and the per-check verdicts.
}
\description{
Runs in the caller's R session against the in-memory data frames named by \code{x}
and \code{y} -- nothing is copied or written to disk.
}
\examples{
orders <- data.frame(id = 1:10, a = 1:10)
lookup <- data.frame(id = 1:8, b = 1:8)
tw_check_join("orders", "lookup", by = "id", type = "inner")
}
