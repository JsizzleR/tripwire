% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{tw_mcp_server}
\alias{tw_mcp_server}
\title{Serve tripwire's checks as an MCP server (stdio)}
\usage{
tw_mcp_server(session_tools = TRUE, ...)
}
\arguments{
\item{session_tools}{Route tool calls into a live R session (default \code{TRUE}).}

\item{...}{Passed on to \code{\link[mcptools:mcp_server]{mcptools::mcp_server()}}.}
}
\value{
Invisibly; runs the server loop until the client disconnects.
}
\description{
Launches a Model Context Protocol server exposing tripwire's checks as tools,
for any MCP client (Claude Code, Claude Desktop, Positron Assistant). Run
non-interactively, e.g. \verb{Rscript -e "tripwire::tw_mcp_server()"}.
}
\details{
With \code{session_tools = TRUE} (default), tool calls are routed into the live R
session the user has registered with \code{\link[mcptools:mcp_session]{mcptools::mcp_session()}}, so checks run
against in-memory data frames by name. Without a registered session, the tools
run in the server process (where the named objects do not exist) and return a
clear error -- use the \verb{*_file} checks for on-disk data in that case.
}
