% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose-missing.R
\name{diagnose_missing}
\alias{diagnose_missing}
\title{Diagnose surprising missingness}
\usage{
diagnose_missing(data, cols = NULL, before = NULL, threshold = 0.1)
}
\arguments{
\item{data}{Data frame to inspect (the "after" state).}

\item{cols}{Optional character vector of columns to check; defaults to all.}

\item{before}{Optional data frame: the same table before the operation. When
given, the check scores the \emph{increase} in missingness rather than its level.}

\item{threshold}{Fraction (0-1) at or above which a column is flagged.}
}
\value{
A \code{tw_check} verdict (status, summary, and the underlying numbers).
}
\description{
Flags columns whose share of missing values is high, or -- when a \code{before}
snapshot is supplied -- whose missingness jumped after an operation.
}
\examples{
after <- data.frame(id = 1:5, region = c("E", NA, NA, "S", "W"))
diagnose_missing(after, threshold = 0.10)
}
