% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose-join.R
\name{diagnose_join}
\alias{diagnose_join}
\title{Diagnose a join for silent row loss or fan-out}
\usage{
diagnose_join(x, y, by, type = c("inner", "left", "right", "full"))
}
\arguments{
\item{x, y}{Data frames to be joined.}

\item{by}{Character vector of one or more shared key column names.}

\item{type}{Join type: \code{"inner"}, \code{"left"}, \code{"right"}, or \code{"full"}.}
}
\value{
A \code{tw_check} verdict (status, summary, and the underlying numbers).
}
\description{
Analyses what a join between \code{x} and \code{y} on \code{by} \emph{would} do, without you
having to eyeball row counts. It reports rows that have no match (and would
be dropped or NA-filled) and rows that would multiply because a key is
duplicated on the other side.
}
\examples{
x <- data.frame(id = 1:10, a = 1:10)
y <- data.frame(id = 1:8, b = 1:8) # ids 9 and 10 have no match
diagnose_join(x, y, by = "id", type = "inner")
}
