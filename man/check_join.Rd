% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-join.R
\name{check_join}
\alias{check_join}
\title{Join two tables and check the join in one step}
\usage{
check_join(x, y, by, type = c("inner", "left", "right", "full"), warn = TRUE)
}
\arguments{
\item{x, y}{Data frames to be joined.}

\item{by}{Character vector of one or more shared key column names.}

\item{type}{Join type: \code{"inner"}, \code{"left"}, \code{"right"}, or \code{"full"}.}

\item{warn}{If \code{TRUE}, emit a warning when the verdict is not \code{"pass"};
otherwise print the verdict.}
}
\value{
The joined data frame, with a \code{tw_check} verdict in its attributes.
}
\description{
A drop-in for a base \code{merge()} that also runs \code{\link[=diagnose_join]{diagnose_join()}} and warns you
when the join silently dropped or multiplied rows. The verdict is attached to
the result as the \code{"tw_check"} attribute.
}
\examples{
x <- data.frame(id = 1:10, a = 1:10)
y <- data.frame(id = 1:8, b = 1:8)
out <- check_join(x, y, by = "id", type = "inner")
attr(out, "tw_check")
}
